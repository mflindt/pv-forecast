# The confirmation run: fit on everything before 2025, test on 2025.

extends: config.yaml
name: holdout

# One fold; n_folds, test_days, and mode have no effect here.
splits:
  holdout: true
  gap_hours: 48
